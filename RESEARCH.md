# 臨床研究のためのデータ項目と使い方ガイド

このダッシュボードを「日々の症例把握」だけでなく「臨床研究のデータ基盤」として使うための提案と手順をまとめる。

## 1. 追加データ項目の提案（シート列 28〜42）

フロントエンド（型・集計・相関分析・CSV出力）と `google_apps_script.js` は下記の列に**すでに対応済み**。
Google スプレッドシートの 29 列目（=インデックス 28）以降に列を追加し、データを入力すれば自動で画面に現れる（3 件以上入力されると相関分析の軸候補に出現、1 件以上で症例一覧の列に出現）。

| 列 | 項目名（推奨ヘッダ） | 入力形式 | 臨床研究上の意義 |
|----|----|----|----|
| 28 | 骨密度 YAM% | 数値（例 `62`） | 日本の骨粗鬆症診断基準（YAM 70%未満等）。重症度の共変量として必須級 |
| 29 | 骨密度 Tスコア | 数値（例 `-3.1`） | 国際比較用（WHO 基準 −2.5）。海外誌投稿を見据えるなら YAM% と併記 |
| 30 | 入院時疼痛NRS | 0〜10 の整数 | ベースライン疼痛。BKP 適応・除痛効果研究の主要変数 |
| 31 | 退院時疼痛NRS | 0〜10 の整数 | 治療効果。「疼痛改善度（入院時−退院時）」は自動で派生される |
| 32 | 入院時Barthel Index | 0〜100 | ベースライン ADL。機能予後研究の基本 |
| 33 | 退院時Barthel Index | 0〜100 | 機能的アウトカム。在院日数・退院先の規定因子 |
| 34 | 退院時歩行能力 | `独歩`/`杖`/`歩行器`/`車椅子`/`寝たきり` | Barthel が取れない場合の実用的アウトカム |
| 35 | 血清アルブミン | 数値 g/dL（例 `3.6`） | 栄養・フレイル指標。在院日数遷延・合併症の予測因子 |
| 36 | 25(OH)ビタミンD | 数値 ng/mL（例 `14`） | 欠乏の頻度調査・骨粗鬆症治療選択の根拠 |
| 37 | 入院中合併症 | カンマ区切り（例 `肺炎, せん妄`）。なければ `なし` | 安全性エンドポイント。`なし` と空欄（未調査）は区別される |
| 38 | 新規隣接椎体骨折 | `あり`/`なし` | OVF・BKP 後の最重要イベントの一つ |
| 39 | 90日以内再入院 | `あり`/`なし` | 医療の質指標（quality indicator） |
| 40 | 死亡 | `なし`/`入院中`/`1年以内` | ハードエンドポイント |
| 41 | 装具 | `硬性`/`軟性`/`なし` | 保存療法プロトコルの記述・比較 |
| 42 | 退院時骨粗鬆症治療薬 | `テリパラチド`/`ロモソズマブ`/`デノスマブ`/`BP`/`SERM`/`その他`/`なし` | 二次予防実施率は国内 OVF 研究の主要テーマ |

入力のポイント:

- **空欄 = 未調査（欠損）** として扱われ、集計・検定から自動で除外される。「該当なし」は `なし` と明記する
- あり/なし系は `あり`/`なし` のほか `Yes`/`No`、`有`/`無` も解釈される
- 列の**順番がすべて**（ヘッダ名ではなくインデックスで読むため）。途中に列を挿入すると以降がズレるので、追加は必ず右端に

### 反映手順

1. スプレッドシートの右端（28列目以降）に上記の列を追加
2. `google_apps_script.js` の内容を Apps Script エディタに貼り直す（28〜42 のマッピングが追加済み）
3. デプロイ → 新しいデプロイ → ウェブアプリ → URL を `NEXT_PUBLIC_GAS_API_URL` に設定
4. 列をまだ追加していなくても新しい GAS をデプロイしてよい（存在しない列は JSON から省かれ、画面側は自動で非表示のまま）

## 2. ダッシュボードの研究での使い方

想定ワークフロー: **コホート定義 → 記述統計 → 仮説探索 → データ出力 → 確証解析**

1. **コホート定義**: ヘッダの「年」と「全体/手術/保存」フィルタで対象集団を絞る。フィルタは全タブに効く
2. **記述統計（Table 1 の素材）**: 概況タブの KPI 行とチャート群が、年齢・性別・骨折レベル・受傷機転・OF分類・在院日数（中央値 [IQR]・平均±SD）をそのまま提供する
3. **仮説探索**: 相関分析タブで
   - 散布図モード: 数値×数値の相関（Pearson r・95%CI・p、Spearman ρ・p、回帰式・R²）。「治療群で色分け」で交互作用の当たりをつける。クアドラントゲート（閾値線）で「高齢×長期入院」のような象限の症例数・割合を即座に確認できる
   - 群間比較モード: カテゴリ×数値（群別 n・平均±SD・中央値 [IQR]、2群なら Welch t 検定と Mann–Whitney U 検定）
4. **データ出力**: 症例一覧タブで検索・フィルタした集合を「CSV出力」。日本語ヘッダ・UTF-8（BOM付き）・CRLF なので Excel / EZR / SPSS にそのまま読める。原文列と派生列（在院日数・術後日数・治療群など）の両方を含む
5. **確証解析**: 多変量解析（在院日数の規定因子、隣接骨折のリスク因子など）や 3 群以上の比較は CSV を EZR / R / SPSS で

### 研究テーマの例（現行データ + 追加項目で可能になるもの）

- 在院日数の規定因子（年齢・BMI・Alb・Barthel・治療法）— 散布図で探索 → 重回帰は CSV で
- BKP の除痛効果と隣接椎体骨折率（NRS 変化・列38）
- 受傷から手術までの待機日数と術後在院日数・退院先の関係
- 骨粗鬆症治療の二次予防実施率（列42）と再骨折・再入院（列38・39）
- ビタミンD 欠乏（列36）の頻度と骨密度・骨折型の関連

## 3. 統計機能の注意点（重要）

- 本ダッシュボードの p 値は**探索的解析**用。多重比較の補正はしていないため、ここで p<0.05 だから論文の結論にできる、とはならない（仮説生成 → 解析計画を立てて確証解析、の流れを推奨）
- Mann–Whitney U は正規近似（タイ補正・連続性補正あり）。群の n<8 では参考値表示になる。Spearman の p も n<10 は参考値
- 期待度数や分散が足りない場合は「データ不足」「計算不可」と表示し、無理な値は出さない
- 在院日数は実データの癖（MM/DD 入力・年跨ぎで負になるシート計算）を補正した値。補正不能なレコードは欠損として除外される

## 4. 運用上の注意

- **匿名化の維持**: ID 列は院内連結可能匿名化 ID のみとし、氏名・カルテ番号そのものを入力しない。MRI リンクも外部公開設定になっていないか確認する
- **倫理手続き**: 後ろ向き観察研究として公表する場合は、倫理委員会承認とオプトアウト文書の掲示が前提（人を対象とする生命科学・医学系研究に関する倫理指針）
- **データの出所は単一に**: 集計ロジックはダッシュボードと CSV で同一（同じ派生計算を共有）なので、論文の数値は CSV → 統計ソフトの出力で確定させ、画面の数値は確認用とする
